name: Trendyol Interview Pipeline

on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  security-events: write

jobs:
  tip:
    name: trendyol-interview-pipeline
    runs-on: ubuntu-latest
    if: (github.actor != 'dependabot[bot]')

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ---------------------------
      # SAST (Semgrep) -> SARIF -> GitHub Security Dashboard
      # ---------------------------
      - name: Semgrep scan (SARIF)
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/ci
          args: semgrep scan --config p/ci --sarif --output=semgrep.sarif

      - name: Upload Semgrep SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: semgrep.sarif
          category: semgrep

      # ---------------------------
      # Secret scanning (Gitleaks) -> SARIF -> GitHub Security Dashboard
      # gitleaks-action outputs results.sarif by default
      # ---------------------------
      - name: Gitleaks Secret Scan (SARIF)
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Gitleaks SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: results.sarif
          category: gitleaks

      # ---------------------------
      # Build container image
      # ---------------------------
      - name: Build Docker image
        run: docker build -t juice-shop:${{ github.sha }} .

      # ---------------------------
      # SBOM (Syft) -> artifact
      # ---------------------------
      - name: Generate SBOM (Syft)
        uses: anchore/sbom-action@v0
        with:
          image: juice-shop:${{ github.sha }}
          format: cyclonedx-json
          output-file: sbom.cdx.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom-cyclonedx
          path: sbom.cdx.json

      # ---------------------------
      # Container vuln scan (Grype via anchore/scan-action) -> SARIF -> GitHub Security Dashboard
      #
      # Fixes:
      # - remove unsupported output-file
      # - prevent failing the job on high vulns (fail-build: false)
      # - upload the default SARIF file name: results.sarif
      # ---------------------------
      - name: Scan image with Grype (SARIF)
        uses: anchore/scan-action@v3
        with:
          image: juice-shop:${{ github.sha }}
          fail-build: "false"
          output-format: sarif
          severity-cutoff: high

      - name: Upload Grype SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: results.sarif
          category: grype

      # Optional: keep the last SARIF around as an artifact (useful for debugging)
      - name: Upload latest SARIF artifact (debug)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: latest-results-sarif
          path: results.sarif
